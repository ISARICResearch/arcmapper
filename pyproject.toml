[project]
name = "arcmapper"
version = "0.1.0"
description = "Automapper library for ISARIC ARC"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
